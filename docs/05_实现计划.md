# 实现计划
# 智能内存优化引擎 (SmartMemEngine)

---

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 智能内存优化引擎实现计划 |
| 文档版本 | 1.0 |
| 作者 | Your Name |
| 日期 | 2026-04-11 |
| 状态 | 草稿 |

---

## 2. 总体实现思路

### 第一阶段：整体框架搭建（可以跑起来）

目标：搭建一个可加载、可卸载的内核模块框架，所有模块的基本结构都创建好，但功能只是桩实现（空实现或简单打印）。

**交付标准**：
- 模块可以成功加载（`insmod smartmem.ko`）
- 模块可以成功卸载（`rmmod smartmem`）
- `/proc/smartmem/` 目录存在，可访问基础文件
- 所有7大功能模块的基本代码框架都存在
- 模块可以编译，无警告

### 第二阶段：逐模块功能实现

目标：按照依赖关系，依次实现每个功能模块的完整功能。

**实现顺序**（按依赖关系排序）：
1. 配置和统计系统（基础）
2. Hook层框架（数据来源）
3. 策略（核心逻辑）
4. 监控系统（数据收集）
5. 分析引擎（数据处理）
6. 优化引擎（自动调优）
7. 接口层完善（用户交互）

---

## 3. 第一阶段：整体框架搭建（第1周）

### 目标
搭建一个可以编译、加载、卸载的完整模块框架

### 任务分解

#### Day 1：目录结构和基本头文件

**任务清单**

**任务1.1：创建目录结构**
```bash
smartmem_engine/
├── Makefile
├── README.md
├── smartmem.h              # 核心头文件
├── smartmem.c              # 模块主入口
├── core/
│   ├── engine.h            # 引擎核心
│   ├── engine.c
│   ├── config.h            # 配置系统
│   ├── config.c
│   └── stats.h             # 统计系统
│       └── stats.c
├── hooks/
│   ├── hook.h              # Hook框架
│   ├── hook.c
│   ├── buddy_hook.h        # Buddy Hook
│   ├── buddy_hook.c
│   ├── slub_hook.h          # SLUB Hook
│   ├── slub_hook.c
│   ├── vma_hook.h           # VMA Hook
│   ├── vma_hook.c
│   ├── lru_hook.h           # LRU Hook
│   ├── lru_hook.c
│   └── numa_hook.h         # NUMA Hook
│       └── numa_hook.c
├── strategy/
│   ├── strategy.h          # 策略引擎
│   ├── strategy.c
│   └── builtin_strategies/ # 内置策略
│       ├── numa_buddy.h
│       ├── numa_buddy.c
│       ├── adaptive_slub.h
│       ├── adaptive_slub.c
│       ├── multi_gen_lru.h
│       ├── multi_gen_lru.c.c
│       ├── numa_balance.h
│       └── numa_balance.c
├── monitor/
│   ├── monitor.h           # 监控系统
│   ├── monitor.c
│   ├── ebpf_monitor.h      # eBPF监控
│   ├── ebpf_monitor.c
│   └── trace_monitor.h     # tracepoint监控
│       └── trace_monitor.c
├── analysis/
│   ├── analysis.h          # 分析引擎
│   ├── analysis.c
│   ├── hotspot.h           # 热点识别
│   ├── hotspot.c
│   ├── bottleneck.h        # 瓶颈分析
│   ├── bottleneck.c
│   └── root_cause.h        # 根因分析
│       └── root_cause.c
├── optimization/
│   ├── optimization.h       # 优化引擎
│   ├── optimization.c
│   ├── auto_tune.h         # 自动调优
│   ├── auto_tune.c
│   └── predictive.h         # 预测模型
│       └── predictive.c
├── interface/
│   ├── interface.h         # 接口层
│   ├── interface.c
│   ├── proc.h              # procfs接口
│   ├── proc.c
│   ├── debugfs.h           # debugfs接口
│   └── debugfs.c
└── tools/                  # 用户空间工具
    ├── memctl.c            # 配置工具
    ├── memstat.c           # 统计工具
    └── memview.c           # 可视化工具
```

**任务1.2：编写smartmem.h核心头文件**
```c
/* smartmem.h - 智能内存优化引擎核心头文件 */

#ifndef _SMARTMEM_H
#define _SMARTMEM_H

#include <linux/module.h>
#include <linux/kernel.h>
#include <linux/init.h>
#include <linux/types.h>
#include <linux/spinlock.h>
#include <linux/atomic.h>
#include <linux/percpu.h>

/* 版本信息 */
#define SMARTMEM_VERSION        "1.0.0"
#define SMARTMEM_NAME           "smartmem"
#define SMARTMEM_DESC           "Smart Memory Optimization Engine"

/* 功能标志位 */
#define SMARTMEM_HOOK_BUDDY     (1 << 0)
#define SMARTMEM_HOOK_SLUB      (1 << 1)
#define SMARTMEM_HOOK_VMA       (1 << 2)
#define SMARTMEM_HOOK_LRU       (1 << 3)
#define SMARTMEM_HOOK_NUMA      (1 << 4)
#define SMARTMEM_HOOK_ALL       0x1F

/* 模块状态 */
enum smartmem_state {
    SMARTMEM_STATE_UNINITIALIZED = 0,
    SMARTMEM_STATE_INITIALIZING,
    SMARTMEM_STATE_READY,
    SMARTMEM_STATE_STOPPING,
    SMARTMEM_STATE_ERROR,
};

/* 全局状态结构 */
struct smartmem_global_state {
    /* 模块状态 */
    enum smartmem_state state;
    bool enabled;

    /* 功能开关 */
    u32 active_hooks;

    /* 统计信息 */
    u64 alloc_count;
    u64 free_count;

    /* 锁 */
    spinlock_t lock;

    /* 引擎指针（指向各子模块） */
    void *engine;
    void *monitor;
    void *analysis;
    void *optimization;
};

/* 外部声明 */
extern struct smartmem_global_state *g_sm_state;

/* 核心接口 */
int smartmem_init(void);
void smartmem_exit(void);
int smartmem_enable(void);
int smartmem_disable(void);

#endif /* _SMARTMEM_H */
```

**任务1.3：编写smartmem.c模块主入口**
```c
/* smartmem.c - 智能内存优化引擎模块主入口 */

#include "smartmem.h"

/* 全局状态 */
struct smartmem_global_state *g_sm_state = NULL;

/**
 * smartmem_init - 模块初始化
 */
static int __init smartmem_init(void)
{
    int ret;

    pr_info("%s: %s initializing...\n", SMARTMEM_NAME, SMARTMEM_DESC);

    /* 分配全局状态 */
    g_sm_state = kzalloc(sizeof(*g_sm_state), GFP_KERNEL);
    if (!g_sm_state) {
        pr_err("%s: failed to allocate global state\n", SMARTMEM_NAME);
        return -ENOMEM;
    }

    spin_lock_init(&g_sm_state->lock);
    g_sm_state->state = SMARTMEM_STATE_INITIALIZING;
    g_sm_state->enabled = false;
    g_sm_state->active_hooks = 0;

    /* TODO: 调用子模块初始化 */

    g_sm_state->state = SMARTMEM_STATE_READY;

    pr_info("%s: initialized successfully\n", SMARTMEM_NAME);
    return 0;
}

/**
 * smartmem_exit - 模块退出
 */
static void __exit smartmem_exit(void)
{
    if (!g_sm_state) {
        return;
    }

    pr_info("%s: exiting...\n", SMARTMEM_NAME);

    g_sm_state->state = SMARTMEM_STATE_STOPPING;

    /* TODO: 调用子模块清理 */

    g_sm_state->state = SMARTMEM_STATE_UNINITIALIZED;

    kfree(g_sm_state);
    g_sm_state = NULL;

    pr_info("%s: exited\n", SMARTMEM_NAME);
}

module_init(smartmem_init);
module_exit(smartmem_exit);

MODULE_LICENSE("GPL");
MODULE_AUTHOR("Your Name");
MODULE_DESCRIPTION(SMARTMEM_DESC);
MODULE_VERSION(SMARTMEM_VERSION);
```

**任务1.4：：编写Makefile**
```makefile
# Makefile for SmartMemEngine

obj-m := smartmem.o

smartmem-y := smartmem.o

# 框架编译通过后取消注释下面的行
# smartmem-y += core/engine.o
# smartmem-y += core/config.o
# smartmem-y += core/stats.o
# smartmem-y += hooks/hook.o
# smartmem-y += hooks/buddy_hook.o
# smartmem-y += hooks/slub_hook.o
# smartmem-y += hooks/vma_hook.o
# smartmem-y += hooks/lru_hook.o
# smartmem-y += hooks/numa_hook.o
# smartmem-y += strategy/strategy.o
# smartmem-y += strategy/builtin_strategies/numa_buddy.o
# smartmem-y += strategy/builtin_strategies/adaptive_slub.o
# smartmem-y += strategy/builtin_strategies/multi_gen_lru.o
# smartmem-y += strategy/builtin_strategies/numa_balance.o
# smartmem-y += monitor/monitor.o
# smartmem-y += monitor/ebpf_monitor.o
# smartmem-y += monitor/trace_monitor.o
# smartmem-y += analysis/analysis.o
# smartmem-y += analysis/hotspot.o
# smartmem-y += analysis/bottleneck.o
# smartmem-y += analysis/root_cause.o
# smartmem-y += optimization/optimization.o
# smartmem-y += optimization/auto_tune.o
# smartmem-y += optimization/predictive.o
# smartmem-y += interface/interface.o
# smartmem-y += interface/proc.o
# smartmem-y += interface/debugfs.o

# 获取内核源码路径
KDIR ?= /lib/modules/$(shell uname -r)/build

all:
    make -C $(KDIR) M=$(PWD) modules

clean:
    make -C $(KDIR) M=$(arsPWD) clean

install:
    make -C $(KDIR) M=$(PWD) modules_install
    depmod -a

.PHONY: all clean install
```

**验收标准**：
- [ ] 所有目录已创建
- [ ] smartmem.h和smartmem.c已创建
- [ ] Makefile已创建
- [ ] `make` 编译成功，生成 smartmem.ko

---

#### Day 2：核心模块框架（engine/config/stats）

**任务清单**

**任务2.1：编写core/engine.h和engine.c**
```c
/* core/engine.h - 引擎核心模块 */

#ifndef _SMARTMEM_ENGINE_H
#define _SMARTMEM_ENGINE_H

#include "../smartmem.h"

/**
 * struct smartmem_engine - 引擎核心结构
 */
struct smartmem_engine {
    /* 配置 */
    void *config;

    /* 统计 */
    void *stats;

    /* 模块状态 */
    bool initialized;
};

/* 引擎接口 */
int smartmem_engine_init(void);
void smartmem_engine_exit(void);
int smartmem_engine_enable(void);
int smartmem_engine_disable(void);

#endif /* _SMARTMEM_ENGINE_H */
```

**任务2.2：编写core/config.h和config.c**
```c
/* core/config.h - 配置系统 */

#ifndef _SMARTMEM_CONFIG_H
#define _SMARTMEM_CONFIG_H

#include "../smartmem.h"

/**
 * struct smartmem_config - 配置结构
 */
struct smartmem_config {
    /* Hook配置 */
    bool hook_buddy_enabled;
    bool hook_slub_enabled;
    bool hook_vma_enabled;
    bool hook_lru_enabled;
    bool hook_numa_enabled;

    /* 策略配置 */
    bool numa_aware_enabled;
    bool adaptive_slub_enabled;

    /* 监控配置 */
    bool ebpf_enabled;
    bool trace_enabled;

    /* 优化配置 */
    bool auto_tune_enabled;

    /* 模块状态 */
    bool initialized;
};

/* 配置接口 */
int smartmem_config_init(void);
void smartmem_config_exit(void);
int smartmem_config_set(const char *key, const char *value);
int smartmem_config_get(const char *key, char *value, size_t len);

#endif /* _SMARTMEM_CONFIG_H */
```

**任务2.3：编写core/stats.h和stats.c**
```c
/* core/stats.h - 统计系统 */

#ifndef _SMARTMEM_STATS_H
#define _SMARTMEM_STATS_H

#include "../smartmem.h"

/**
 * struct smartmem_stats - 统计结构
 */
struct smartmem_stats {
    /* Buddy统计 */
    atomic64_t buddy_alloc_count;
    atomic64_t buddy_free_count;

    /* SLUB统计 */
    atomic64_t slub_alloc_count;
    atomic64_t slub_free_count;

    /* NUMA统计 */
    atomic64_t numa_local_alloc;
    atomic64_t numa_remote_alloc;

    /* 模块状态 */
    bool initialized;
};

/* per-CPU统计 */
struct smartmem_cpu_stats {
    u64 buddy_alloc_count;
    u64 slub_alloc_count;
};

/* 统计接口 */
int smartmem_stats_init(void);
void smartmem_stats_exit(void);
void smartmem_stats_buddy_alloc_inc(void);
void smartmem_stats_buddy_free_inc(void);
void smartmem_stats_slub_alloc_inc(void);
void smartmem_stats_slub_free_inc(void);
void smartmem_stats_reset(void);

#endif /* _SMARTMEM_STATS_H */
```

**任务2.4：更新smartmem.c，连接核心模块**
```c
/* 在smartmem_init中添加 */
ret = smartmem_config_init();
if (ret) {
    pr_err("%s: config init failed\n", SMARTMEM_NAME);
    goto err_config;
}

ret = smartmem_stats_init();
if (ret) {
    pr_err("%s: stats init failed\n", SMARTMEM_NAME);
    goto err_stats;
}

ret = smartmem_engine_init();
if (ret) {
    pr_err("%s: engine init failed\n", SMARTMEM_NAME);
    goto err_engine;
}

/* 在smartmem_exit中添加 */
smartmem_engine_exit();
smartmem_stats_exit();
smartmem_config_exit();
```

**验收标准**：
- [ ] core/engine.h/engine.c已创建
- [ ] core/config.h/config.c已创建
- [ ] core/stats.h/stats.c已创建
- [ ] 模块可以编译成功
- [ ] 模块可以加载和卸载

---

#### Day 3：Hook层框架

**任务清单**

**任务3.1：编写hooks/hook.h和hook.c（Hook管理框架）**
```c
/* hooks/hook.h - Hook管理框架 */

#ifndef _SMARTMEM_HOOK_H
#define _SMARTMEM_HOOK_H

#include "../smartmem.h"

/**
 * enum smartmem_hook_type - Hook类型
 */
enum smartmem_hook_type {
    SMARTMEM_HOOK_TYPE_BUDDY = 0,
    SMARTMEM_HOOK_TYPE_SLUB,
    SMARTMEM_HOOK_TYPE_VMA,
    SMARTMEM_HOOK_TYPE_LRU,
    SMARTMEM_HOOK_TYPE_NUMA,
    SMARTMEM_HOOK_TYPE_MAX,
};

/**
 * struct smartmem_hook_ops - Hook操作函数
 */
struct smartmem_hook_ops {
    int (*init)(void);
    void (*exit)(void);
    int (*enable)(void);
    int (*disable)(void);
};

/**
 * struct smartmem_hook - Hook描述
 */
struct smartmem_hook {
    char name[32];
    enum smartmem_hook_type type;
    struct smartmem_hook_ops ops;
    bool enabled;
    struct list_head list;
};

/* Hook管理接口 */
int smartmem_hook_init(void);
void smartmem_hook_exit(void);
int smartmem_hook_register(struct smartmem_hook *hook);
int smartmem_hook_unregister(struct smartmem_hook *hook);
int smartmem_hook_enable(enum smartmem_hook_type type);
int smartmem_hook_disable(enum smartmem_hook_type type);

#endif /* _SMARTMEM_HOOK_H */
```

**任务3.2：编写hooks/buddy_hook.h和buddy_hook.c**
```c
/* hooks/buddy_hook.h - Buddy分配Hook */

#ifndef _SMARTMEM_BUDDY_HOOK_H
#define _SMARTMEM_BUDDY_HOOK_H

#include "hook.h"

/* Buddy Hook接口 */
int buddy_hook_init(void);
void buddy_hook_exit(void);

#endif /* _SMARTMEM_BUDDY_HOOK_H */
```

**任务3.3：编写hooks/slub_hook.h和slub_hook.c**
```c
/* hooks/slub_hook.h - SLUB分配Hook */

#ifndef _SMARTMEM_SLUB_HOOK_H
#define _SMARTMEM_SLUB_HOOK_H

#include "hook.h"

/* SLUB Hook接口 */
int slub_hook_init(void);
void slub_hook_exit(void);

#endif /* _SMARTMEM_SLUB_HOOK_H */
```

**任务3.4：编写hooks/vma_hook.h、lru_hook.h、numa_hook.h**
```c
/* hooks/vma_hook.h - VMA管理Hook */
#ifndef _SMARTMEM_VMA_HOOK_H
#define _SMARTMEM_VMA_HOOK_H
#include "hook.h"
int vma_hook_init(void);
void vma_hook_exit(void);
#endif

/* hooks/lru_hook.h - LRU管理Hook */
#ifndef _SMARTMEM_LRU_HOOK_H
#define _SMARTMEM_LRU_HOOK_H
#include "hook.h"
int lru_hook_init(void);
void lru_hook_exit(void);
#endif

/* hooks/numa_hook.h - NUMA管理Hook */
#ifndef _SMARTMEM_NUMA_HOOK_H
#define _SMARTMEM_NUMA_HOOK_H
#include "hook.h"
int numa_hook_init(void);
void numa_hook_exit(void);
#endif
```

**验收标准**：
- [ ] hooks/hook.h/hook.c已创建
- [ ] hooks/buddy_hook.h/buddy_hook.c已创建
- [ ] hooks/slub_hook.h/slub_hook.c已创建
- [ ] hooks/vma_hook.h/vma_hook.c已创建
- [ ] hooks/lru_hook.h/lru_hook.c已创建
- [ ] hooks/numa_hook.h/numa_hook.c已创建
- [ ] 模块可以编译成功

---

#### Day 4：策略引擎框架

**任务清单**

**任务4.1：编写strategy/strategy.h和strategy.c**
```c
/* strategy/strategy.h - 策略引擎 */

#ifndef _SMARTMEM_STRATEGY_H
#define _SMARTMEM_STRATEGY_H

#include "../smartmem.h"

/**
 * struct buddy_strategy_ops - Buddy策略操作
 */
struct buddy_strategy_ops {
    int (*pre_alloc)(int order, u32 *gfp_mask, int *nid);
    int (*post_alloc)(void *page, int order, u32 gfp_mask);
    int (*pre_free)(void *page, int order);
    int (*post_free)(int order);
    int (*select_node)(int order) (*gfp_mask, int *nid);
};

/**
 * struct buddy_strategy - Buddy策略
 */
struct buddy_strategy {
    char name[64];
    char version[16];
    char description[128];
    struct buddy_strategy_ops ops;
    bool enabled;
    struct list_head list;
};

/* SLUB策略操作（类似定义）... */

/* 策略引擎接口 */
int smartmem_strategy_init(void);
void smartmem_strategy_exit(void);
int buddy_strategy_register(struct buddy_strategy *s);
int buddy_strategy_unregister(struct buddy_strategy *s);
struct buddy_strategy *buddy_strategy_get_current(void);
int buddy_strategy_switch(const char *name);

#endif /* _SMARTMEM_STRATEGY_H */
```

**任务4.2：编写内置策略头文件框架**
```c
/* strategy/builtin_strategies/numa_buddy.h */
#ifndef _NUMA_BUDDY_H
#define _NUMA_BUDDY_H
#include "../strategy.h"
struct buddy_strategy *numa_buddy_strategy_create(void);
#endif

/* strategy/builtin_strategies/adaptive_slub.h */
#ifndef _ADAPTIVE_SLUB_H
#define _ADAPTIVE_SLUB_H
#include "../strategy.h"
struct slub_strategy *adaptive_slub_strategy_create(void);
#endif

/* strategy/builtin_strategies/multi_gen_lru.h */
#ifndef _MULTI_GEN_LRU_H
#define _MULTI_GEN_LRU_H
#include "../strategy.h"
struct lru_strategy *multi_gen_lru_strategy_create(void);
#endif

/* strategy/builtin_strategies/numa_balance.h */
#ifndef _NUMA_BALANCE_H
#define _NUMA_BALANCE_H
#include "../strategy.h"
struct numa_strategy *numa_balance_strategy_create(void);
#endif
```

**验收标准**：
- [ ] strategy/strategy.h/strategy.c已创建
- [ ] 4个内置策略头文件已创建
- [ ] 模块可以编译成功

---

#### Day 5：监控系统框架

**任务清单**

**任务5.1：编写monitor/monitor.h和monitor.c**
```c
/* monitor/monitor.h - 监控系统 */

#ifndef _SMARTMEM_MONITOR_H
#define _SMARTMEM_MONITOR_H

#include "../smartmem.h"

/**
 * struct smartmem_monitor - 监控系统
 */
struct smartmem_monitor {
    bool ebpf_enabled;
    bool trace_enabled;
    bool initialized;
};

/* 监控接口 */
int smartmem_monitor_init(void);
void smartmem_monitor_exit(void);
int smartmem_monitor_start(void);
int smartmem_monitor_stop(void);

#endif /* _SMARTMEM_MONITOR_H */
```

**任务5.2：编写monitor/ebpf_monitor.h和ebpf_monitor.c**
```c
/* monitor/ebpf_monitor.h - eBPF监控 */

#ifndef _SMARTMEM_EBPF_MONITOR_H
#define _SMARTMEM_EBPF_MONITOR_H

#include "monitor.h"

/* eBPF监控接口 */
int ebpf_monitor_init(void);
void ebpf_monitor_exit(void);
int ebpf_monitor_load(void);
int ebpf_monitor_unload(void);

#endif /* _SMARTMEM_EBPF_MONITOR_H */
```

**任务5.3：编写monitor/trace_monitor.h和trace_monitor.c**
```c
/* monitor/trace_monitor.h - tracepoint监控 */

#ifndef _SMARTMEM_TRACE_MONITOR_H
#define _SMARTMEM_TRACE_MONITOR_H

#include "monitor.h"

/* tracepoint监控接口 */
int trace_monitor_init(void);
void trace_monitor_exit(void);
int trace_monitor_start(void);
int trace_monitor_stop(void);

#endif /* _SMARTMEM_TRACE_MONITOR_H */
```

**验收标准**：
- [ ] monitor/monitor.h/monitor.c已创建
- [ ] monitor/ebpf_monitor.h/ebpf_monitor.c已创建
- [ ] monitor/trace_monitor.h/trace_monitor.c已创建
- [ ] 模块可以编译成功

---

#### Day 6：分析引擎和优化引擎框架

**任务清单**

**任务6.1：编写analysis/analysis.h和analysis.c**
```c
/* analysis/analysis.h - 分析引擎 */

#ifndef _SMARTMEM_ANALYSIS_H
#define _SMARTMEM_ANALYSIS_H

#include "../smartmem.h"

/**
 * struct smartmem_analysis - 分析引擎
 */
struct smartmem_analysis {
    bool hotspot_enabled;
    bool bottleneck_enabled;
    bool initialized;
};

/* 分析接口 */
int smartmem_analysis_init(void);
void smartmem_analysis_exit(void);
int smartmem_analysis_hotspot_update(void);
int smartmem_analysis_bottleneck_update(void);

#endif /* _SMARTMEM_ANALYSIS_H */
```

**任务6.2：编写analysis/hotspot.h/hotspot.c、bottleneck.h/bottleneck.c、root_cause.h/root_cause.c**

**任务6.3：编写optimization/optimization.h和optimization.c**
```c
/* optimization/optimization.h - 优化引擎 */

#ifndef _SMARTMEM_OPTIMIZATION_H
#define _SMARTMEM_OPTIMIZATION_H

#include "../smartmem.h"

/**
 * struct smartmem_optimization - 优化引擎
 */
struct smartmem_optimization {
    bool auto_tune_enabled;
    bool predictive_enabled;
    bool initialized;
};

/* 优化接口 */
int smartmem_optimization_init(void);
void smartmem_optimization_exit(void);
int smartmem_optimization_start(void);

#endif /* _SMARTMEM_OPTIMIZATION_H */
```

**任务6.4：编写optimization/auto_tune.h/auto_tune.c和predictive.h/predictive.c**

**验收标准**：
- [ ] analysis目录下所有文件已创建
- [ ] optimization目录下所有文件已创建
- [ ] 模块可以编译成功

---

#### Day 7：接口层框架

**任务清单**

**任务7.1：编写interface/interface.h和interface.c**

**任务7.2：编写interface/proc.h和proc.c（基础的procfs接口）**
```c
/* interface/proc.h - procfs接口 */

#ifndef _SMARTMEM_PROC_H
#define _SMARTMEM_PROC_H

#include "../smartmem.h"

/* procfs接口 */
int smartmem_proc_init(void);
void smartmem_proc_exit(void);

#endif /* _SMARTMEM_PROC_H */
```

**任务7.3：编写interface/debugfs.h和debugfs.c**

**验收标准**：
- [ ] interface目录下所有文件已创建
- [ ] 模块可以编译成功
- [ ] 加载模块后 `/proc/smartmem/` 目录存在

---

### 第一阶段验收标准

- [ ] 模块可以成功编译（`make`）
- [ ] 模块可以成功加载（`insmod smartmem.ko`）
- [ ] 模块可以成功卸载（`rmmod smartmem`）
- [ ] `/proc/smartmem/` 目录存在
- [ ] 所有7大功能模块的代码框架都存在
- [ ] 模块无编译警告和错误

---

## 4. 第二阶段：功能模块逐个实现

### 模块实现顺序（按依赖关系）

1. **配置和统计系统**（所有模块依赖的基础）
2. **Hook层**（提供数据源）
3. **策略引擎**（核心优化逻辑）
4. **监控系统**（收集Hook层数据）
5. **分析引擎**（处理监控数据）
6. **优化引擎**（根据分析结果优化）
7. **接口层完善**（提供完整用户接口）

---

## 5. 模块1：配置和统计系统实现（第2周）

### Day 8-9：配置系统完整实现

**任务清单**

**任务8.1：实现smartmem_config_set函数**
- 解析配置键
- 验证配置值
- 更新配置结构
- 返回结果

**任务8.2：实现smartmem_config_get函数**
- 查找配置键
- 格式化配置值
- 返回结果

**任务8.3：实现配置验证函数**
- 检查配置范围
- 检查配置冲突
- 返回验证结果

**验收标准**：
- [ ] 可以设置配置
- [ ] 可以读取配置
- [ ] 配置验证正常工作
- [ ] 测试用例通过

---

### Day 10-11：统计系统完整实现

**任务清单**

**任务10.1：实现per-CPU统计**
- 定义per-CPU统计变量
- 实现per-CPU统计更新函数
- 实现per-CPU到全局统计的聚合函数

**任务10.2：实现统计更新函数**
- smartmem_stats_buddy_alloc_inc()
- smartmem_stats_buddy_free_inc()
- smartmem_stats_slub_alloc_inc()
- smartmem_stats_slub_free_inc()

**任务10.3：实现统计重置函数**
- smartmem_stats_reset()

**验收标准**：
- [ ] 统计更新正常工作
- [ ] per-CPU统计聚合正常
- [ ] 统计重置正常
- [ ] 测试用例通过

---

### Day 12-14：procfs接口 - 配置和统计

**任务清单**

**任务12.1：实现/proc/smartmem/config**
- 实现read回调：显示所有配置
- 实现write回调：设置配置

**任务12.2：实现/proc/smartmem/stats**
- 实现read回调：显示所有统计
- 聚合per-CPU统计
- 格式化输出

**验收标准**：
- [ ] cat /proc/smartmem/config 显示配置
- [ ] echo "key=value" > /proc/smartmem/config 设置配置
- [ ] cat /proc/smartmem/stats 显示统计
- [ ] 统计数据准确

---

## 6. 模块2：Hook层实现（第3周）

### Day 15-17：Buddy Hook实现

**任务清单**

**任务15.1：实现Buddy Hook的kprobe**
- 定义kprobe结构
- 实现pre_handler（入口）
- 实现post_handler（返回）

**任务15.2：实现参数缓存**
- 使用per-CPU变量缓存参数
- 在入口保存时参数
- 在出口获取返回值

**任务15.3：实现Hook安装和卸载**
- buddy_hook_init(): 注册kprobe
- buddy_hook_exit(): 注销kprobe

**验收标准**：
- [ ] Buddy Hook成功安装
- [ ] 分配事件被正确捕获
- [ ] 统计数据正确更新

---

### Day 18-20：SLUB Hook实现

**任务清单**

**任务18.1：实现SLUB Hook的kprobe/kretprobe**
- 定义kprobe和kretprobe结构
- 实现入口和返回处理函数

**任务18.2：实现SLUB统计**
- 分配计数
- 释放计数
- 缓存命中率

**验收标准**：
- [ ] SLUB Hook成功安装
- [ ] kmalloc/kfree事件被捕获
- [ ] 统计数据准确

---

### Day 21：VMA Hook、LRU Hook、NUMA Hook框架实现

**任务清单**

**任务21.1：实现VMA Hook**
- Hook mmap相关函数
- 实现基本统计

**任务21.2：实现LRU Hook**
- Hook mark_page_accessed
- 实现页面访问统计

**任务21.3：实现NUMA Hook**
- Hook migrate_pages
- 实现NUMA统计

**验收标准**：
- [ ] 所有Hook框架实现
- [ ] Hook可以成功安装
- [ ] 基本统计正常工作

---

## 7. 模块3：策略引擎和内置策略实现（第4-5周）

### Day 22-24：策略引擎核心实现

**任务清单**

**任务22.1：实现策略注册**
- buddy_strategy_register()
- 验证策略有效性
- 添加到策略链表
- 设置为当前策略

**任务22.2：实现策略注销**
- buddy_strategy_unregister()
- 从链表移除
- 清理资源

**任务22.3：实现策略查询和切换**
- buddy_strategy_get_current()
- buddy_strategy_switch()

**验收标准**：
- [ ] 策略注册/注销正常
- [ ] 策略查询正常
- [ ] 策略切换正常
- [ ] 测试用例通过

---

### Day 25-28：NUMA感知Buddy策略实现

**任务清单**

**任务25.1：实现pre_alloc回调**
- 获取本地NUMA节点
- 检查是否需要强制本地分配
- 修改gfp_mask

**任务25.2：实现select_node回调**
- 选择最优NUMA节点
- 考虑本地性和内存可用性

**任务25.3：实现策略统计**
- 本地分配计数
- 远程分配计数
- 强制本地分配计数

**验收标准**：
- [ ] 策略可以注册
- [ ] 预分配回调正常工作
- [ ] NUMA本地性得到提升
- [ ] 统计数据准确

---

### Day 29-32：自适应SLUB策略实现

**任务清单**

**任务29.1：实现post_alloc回调**
- 检查是否命中per-CPU缓存
- 更新命中率统计

**任务29.2：实现缓存大小自适应**
- 根据命中率调整缓存大小
- 动态调优逻辑

**验收标准**：
- [ ] 策略可以注册
- [ ] 缓存命中率统计准确
- [ ] 自适应调优正常工作

---

### Day 33-35：其他内置策略框架实现

**任务清单**

**任务33.1：实现多代LRU策略框架**
- 定义多代LRU结构
- 实现页面访问跟踪

**任务33.2：实现NUMA负载均衡策略框架**
- 实现节点负载监控
- 实现迁移决策逻辑

**验收标准**：
- [ ] 策略框架完整
- [ ] 可以注册和调用
- [ ] 基本功能正常

---

## 8. 模块4：监控系统实现（第6周）

### Day 36-38：eBPF监控实现

**任务清单**

**任务36.1：编写eBPF程序**
- 定义BPF map
- 实现kprobe处理函数
- 实现调用栈捕获

**任务36.2：实现eBPF程序加载**
- 编译eBPF程序
- 加载到内核
- 附加kprobe

**任务36.3：实现用户空间读取**
- 定期读取BPF map
- 解析数据
- 更新统计

**验收标准**：
- [ ] eBPF程序编译成功
- [ ] 可以加载到内核
- [ ] BPF map数据可以读取
- [ ] 监控数据准确

---

### Day 39-40：tracepoint监控实现

**任务清单**

**任务39.1：订阅内存管理tracepoint**
- mm_page_alloc
- mm_page_free
- mm_pagefault

**任务39.2：实现tracepoint处理函数**
- 解析tracepoint参数
- 更新统计

**验收标准**：
- [ ] tracepoint订阅成功
- [ ] 事件被正确捕获
- [ ] 统计正常更新

---

### Day 41-42：监控聚合和procfs输出

**任务清单**

**任务41.1：实现监控数据聚合**
- 从eBPF map读取
- 从tracepoint读取
- 聚合到全局统计

**任务41.2：实现/proc/smartmem/hotspots**
- 显示Top-N热点调用栈
- 格式化输出

**验收标准**：
- [ ] 监控数据聚合正常
- [ ]/proc/smartmem/hotspots可用
- [ ] 热点数据准确

---

## 9. 模块5：分析引擎实现（第7周）

### Day 43-46：热点识别实现

**任务清单**

**任务43.1：实现调用栈hash计算**
- 获取调用栈
- 计算hash值

**任务43.2：实现热点聚合**
- 按hash聚合统计
- 维护热点列表

**任务43.3：实现热点评分**
- 访问频率得分
- 内存占用得分
- 时间衰减得分
- 综合评分

**任务43.4：实现热点排序**
- 按综合评分排序
- 维护Top-N列表

**验收标准**：
- [ ] 调用栈hash计算正确
- [ ] 热点聚合正常
- [ ] 热点评分合理
- [ ] Top-N热点准确

---

### Day 47-48：瓶颈分析和根因分析框架

**任务清单**

**任务47.1：实现瓶颈分析框架**
- 延迟统计
- 长尾识别

**任务47.2：实现根因分析框架**
- 问题分类
- 原因推断

**验收标准**：
- [ ] 分析框架完整
- [ ] 基本功能正常

---

## 10. 模块6：优化引擎实现（第8周）

### Day 49-52：自动调优实现

**任务清单**

**任务49.1：实现调优触发检测**
- 检测NUMA远程访问率
- 检测SLUB命中率
- 检测内存碎片

**任务49.2：实现调优动作**
- 触发页面迁移
- 调整策略参数
- 触发内存整理

**任务49.3：实现netlink告警**
- 构建netlink消息
- 发送告警

**验收标准**：
- [ ] 调优触发检测正常
- [ ] 调优动作执行成功
- [ ] netlink告警发送正常

---

### Day 53-54：预测模型框架

**任务清单**

**任务53.1：实现简单的预测模型**
- 基于历史数据的趋势预测
- OOM预测

**验收标准**：
- [ ] 预测框架完整
- [ ] 基本预测功能正常

---

## 11. 模块7：接口层完善（第9周）

### Day 55-57：完善procfs接口

**任务清单**

**任务55.1：实现/proc/smartmem/policies**
- 显示当前策略
- 显示策略统计

**任务55.2：实现/proc/smartmem/control**
- 启用/禁用功能
- 切换策略

**验收标准**：
- [ ] 所有proc接口完整
- [ ] 接口功能正常

---

### Day 58-59：完善debugfs接口

**任务清单**

**任务58.1：实现调试接口**
- 状态dump
- 内部数据导出

**验收标准**：
- [ ] debugfs接口完整
- [ ] 调试功能正常

---

### Day 60-62：用户空间工具

**任务清单**

**任务60.1：实现memctl工具**
- 配置管理

**任务60.2：实现memstat工具**
- 统计查看

**任务60.3：实现memview工具**
- 可视化展示

**验收标准**：
- [ ] 所有工具可编译
- [ ] 工具功能正常

---

## 12. 测试和打磨（第10周）

### Day 63-65：集成测试

**任务清单**

- 测试完整流程
- 测试模块协同
- 测试边界条件
- 性能测试

### Day 66-70：最终打磨

**任务清单**

- 修复所有bug
- 优化性能
- 完善文档
- 准备演示

---

## 13. 里程碑

| 里程碑 | 时间 | 交付物 |
|--------|------|--------|
| M1：框架完成 | 第1周 | 可加载的完整模块框架 |
| M2：配置统计完成 | 第2周 | 配置和统计系统完整实现 |
| M3：Hook层完成 | 第3周 | 所有Hook基本实现 |
| M4：策略引擎完成 | 第5周 | 策略引擎+内置策略 |
| M5：监控系统完成 | 第6周 | eBPF+tracepoint监控 |
| M6：分析优化完成 | 第8周 | 分析+优化引擎 |
| M7：接口完成 | 第9周 | 完整的接口和工具 |
| M8：项目完成 | 第10周 | 可交付的完整项目 |

---

## 14. 时间估算

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| 框架搭建 | 1周（7天） | 可加载的框架 |
| 配置统计系统 | 1周（7天） | 配置和统计系统 |
| Hook层 | 1周（7天） | 完整的Hook层 |
| 策略引擎 | 2周（14天） | 策略引擎+内置策略 |
| 监控系统 | 1周（7天） | eBPF+tracepoint监控 |
| 分析引擎 | 1周（6天） | 热点识别+分析 |
| 优化引擎 | 1周（6天） | 自动调优+预测 |
| 接口层 | 1周（8天） | 完整接口+工具 |
| 测试打磨 | 1周（8天） | 测试+文档 |

**总时间：10周（70天）**

---

**文档版本：** 1.0
**最后更新：** 2026-04-11
